info:
  name: 11 Me After Logout
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:3000/api/users/me

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 401 without auth", function() {
          expect(res.status).to.equal(401);
        });
