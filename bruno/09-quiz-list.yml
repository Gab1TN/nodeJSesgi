info:
  name: 09 Quiz List
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:3000/api/quizzes

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });
