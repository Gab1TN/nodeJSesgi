info:
  name: 12 Me With Avatar Note
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:3000/api/users/me
  headers:
    - name: Authorization
      value: Bearer {{token}}

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });
