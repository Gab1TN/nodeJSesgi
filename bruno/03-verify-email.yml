info:
  name: 03 Verify Email
  type: http
  seq: 3

http:
  method: GET
  url: "{{verificationLink}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 200", function() {
          expect(res.status).to.equal(200);
        });
