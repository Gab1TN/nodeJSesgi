info:
  name: 02 Login Before Verify
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/users/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "{{testEmail}}",
        "password": "secret123"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 403 before email verify", function() {
          expect(res.status).to.equal(403);
        });
